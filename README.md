# 健身记 Gymnota - 个人健身记录备忘录 (V5.8)

**健身记** 是一款基于 Android Jetpack Compose 开发的个人健身追踪和备忘记录应用。它旨在帮助用户规划周度训练方案、记录每日打卡，并通过**原生图表**深度可视化追踪训练容量与身体状态变化。

🚀 **V5.8 版本重磅更新**：新增**训练容量热力图 (Heatmap)**，直观展示历史训练频率与负荷；重构**单边动作 (Unilateral)** 数据统计逻辑。
---

[English Version](https://github.com/enersto/Gymnota/blob/main/READEME-en.md)

## ✨ 核心功能 (Features)

**🔥 训练容量热力图 (Training Volume Heatmap)** `NEW`
* **可视化分布**：在历史页面新增热力图组件，通过色块深浅直观展示每日的训练强度。
* **容量定义**：采用「历史累积容量负荷 (重量 × 次数)」作为统计标准。
* **UI 优化**：全新设计的底部图例区域，采用水平居中布局，提示文字清晰易读。

**⚖️ 智能数据逻辑 (Smart Data Logic)** `NEW`
* **单边动作合并**：全面重构数据处理逻辑，针对左右分侧的单边动作（如单臂哑铃划船），统计时自动识别并正确合并计算，确保总容量数据准确无误。

**💪 身体状态与指标管理 (Body Stats & Metrics)**
* **智能记录**：打卡页新增「记录身体情况」入口，支持记录体重、身高、年龄及性别。
* **自动计算**：系统根据录入的基础信息，自动计算每日的 **BMI (身体质量指数)** 和 **BMR (基础代谢率)**。
* **隐私保护**：基础信息（身高/年龄/性别）仅存储在本地数据库，支持在设置页随时修改。

 **⏱️ 专业级训练计时器 (Pro Workout Timer)**
* **前台服务 (Foreground Service)**：重构计时逻辑，计时器在锁屏、熄屏或后台运行时不再会被系统杀掉。
* **锁屏常驻**：通知栏实时显示倒计时秒数，支持在锁屏界面直接查看剩余时间。
* **权限引导**：新增「锁屏通知」权限的智能引导弹窗。

 **📈 可视化统计中心 (Visual Statistics Center)**
* **原生图表**：基于 Compose Canvas 绘制的高性能折线图与柱状图。
  
 **多维分析**：
* **身体状态**：追踪体重、BMI 和 BMR 的长期变化曲线。
* **有氧/力量**：统计每日/月有氧总时长，以及单项动作的最大重量或训练次数走势。
* **视图切换**：支持「日/月」粒度切换。

 **🔄 智能数据导入 (Smart CSV Import)**
* **自动收录**：导入周计划 CSV 时，若包含新动作，系统将自动创建模板并补充部位/器械信息。
* **智能去重**：导入时自动检测并清理动作库中的重复数据。

 **🌍 多语言支持 (Internationalization)**
* 完整支持 **简体中文、English、Deutsch、Español、日本語**。
* **深度适配**：V5.8.1 新增热力图提示语「数值为历史累积容量负荷」的多语言翻译，界面文案全方位国际化。

 **📅 智能周度方案 (Smart Weekly Routine)**
* 可视化界面安排每周固定日的训练动作，自动生成每日待办任务。
* 支持为每天设定类型标签（如核心日、休息日），首页根据类型自动变换主题色。

---

## 📱 使用指南 (User Guide)

### 1. 查看训练热力图 `NEW`

* 进入 **历史 (History)** 页面。
* 页面顶部即为 **训练容量热力图**。
* **图例说明**：底部图例展示了从低到高的容量级别，文字提示「数值为历史累积容量负荷 (重量 × 次数)」会自动适配当前系统语言。

### 2. 身体数据记录

* 在首页点击日期下方的 **「记录身体情况」** 按钮。
* **首次使用**：弹窗会要求输入身高、年龄、性别和体重，用于建立基础档案。
* **日常记录**：后续只需输入体重即可，系统会自动复用基础信息计算 BMI/BMR。

### 3. 训练计时与锁屏显示

* 在动作组右侧输入时间（分钟），点击播放键开始计时。
* **锁屏查看**：
    * 计时开始后，您可以直接锁屏。
    * 点亮屏幕（无需解锁）即可在通知区域看到倒计时。
    * *注：若未显示，请按 App 弹窗指引在系统设置中开启「锁屏通知」权限。*

### 4. 管理动作库 & 周计划

* 进入 **设置 (Settings)** 页面。
* **动作库**：通过三级分类（类别->部位->器械）高效管理动作。
* **周计划**：手动勾选或通过 CSV 批量导入每周固定训练。

---

## 📂 CSV 导入格式说明

支持通过导入 CSV 自动扩充动作库。推荐使用以下完整格式：

`星期(1-7), 动作名称, 类别(STRENGTH/CARDIO/CORE), 目标, 部位Key, 器械Key`

**Key 参考**：

**Key 参考**：

* **部位**:
    * 上肢/躯干: `part_chest`, `part_back`, `part_shoulders`, `part_arms`, `part_abs`
    * 下肢: `part_hips` (臀部), `part_thighs` (大腿), `part_calves` (小腿)
    * 其他: `part_cardio`, `part_other`
* **器械**: `equip_barbell`, `equip_dumbbell`, `equip_machine`, `equip_cable`, `equip_smith_machine`, `equip_bodyweight`, `equip_cardio_machine`, `equip_other`

**示例：**

```text
1, 杠铃卧推, STRENGTH, 4x8, part_chest, equip_barbell
3, 波比跳, CORE, 4x15, part_abs, equip_bodyweight

```

---

## 🛠️ 技术栈 (Tech Stack)

* **Language**: Kotlin
* **UI Framework**: Jetpack Compose (Material3)
* **Architecture**: MVVM (Model-View-ViewModel)
* **Database**: Room (SQLite) with Migrations (v9)
* **Service**: Android Foreground Service (Keep-alive Timer)
* **Graphics**: Compose Canvas (Custom Charts)
* **Concurrency**: Coroutines & Flow

---

## 📝 版本信息

* **Version**: 5.8
* **Version Code**: 58
* **Author**: Designed & Built by enersto & Hajimi

---
